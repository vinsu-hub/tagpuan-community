# Inner-route logo variant QA

Representative `/about`, `/events`, and `/join` desktop captures confirm the hut-only mark is visible and proportionate in the shared header, while the full transparent lockup is reserved for the spacious homepage footer. The three routes retain consistent navigation spacing, reminder ribbon hierarchy, hero contrast, and scrapbook-colored content surfaces. The remaining mobile route behavior is covered by the shared responsive nav implementation and the prior mobile homepage verification pass.
