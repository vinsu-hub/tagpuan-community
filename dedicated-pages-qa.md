# Dedicated full-page tab QA

The homepage primary tabs now use route URLs: `/about`, `/events`, `/wall`, and `/join`. The shared inner-page shell also exposes `/projects` and `/people`. Desktop and 390px mobile captures confirm every route renders a distinct full-page experience with its own hero, content body, reminder ribbon, responsive menu, and return path. The mobile views preserve the Tagpuan hierarchy while allowing Events cards and Wall notes to continue as tactile horizontal/board layouts.
