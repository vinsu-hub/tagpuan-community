# Recap motion QA

The Last time at Tagpuan recap posters now use a dedicated `recap-photo-in` animation that restores the earlier scrapbook behavior: a soft upward entrance, small rotation, subtle scale settle, and staggered timing across the photo cards. The rule is placed after the generic element reveal so it is no longer overridden.

Desktop and mobile full-page captures confirm the recap tiles remain visible, proportionate, and visually integrated with the textured events section. The animation keeps the photo-frame character and remains disabled under `prefers-reduced-motion` while preserving the final rotated layout.
